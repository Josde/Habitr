import 'package:flutter/material.dart';

class RoutineRepositoryScreen extends StatelessWidget {
  const RoutineRepositoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
